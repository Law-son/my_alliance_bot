const transporter = require('./mail.services');


class BotServices8 {
    static async fetchTutorDataById(tutorID) {
        try {
            
        } catch (error) {
            throw error; 
        }
    }
}

module.exports = BotServices8;
